configs:
  glance:
    api_port: 9292
    registry_port: 9191

    db:
      username: glance
      password: password
      name: glance

    user: glance
    password: password

    debug: false

  ceph:
    glance_enable: false
    glance_key: "Changeme"
    glance_username: "glance"
    glance_pool_name: "images"

  swift:
    glance_enable: false
    glance_store_create_container_on_put: true
    glance_store_container: glance

sources:
  openstack/glance:
    git_url: https://github.com/openstack/glance.git
    git_ref: stable/newton
