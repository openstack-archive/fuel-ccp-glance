configs:
  glance:
    api_port: 9292
    registry_port: 9191

    db:
      username: glance
      password: password
      name: glance

    user: glance
    password: password

    debug: false

    ceph:
      enable: false
      key: "Changeme"
      username: "glance"
      pool_name: "images"

sources:
  openstack/glance:
    git_url: https://github.com/openstack/glance.git
    git_ref: stable/newton
